#!/usr/bin/env -S scheme --libdirs src:tests:. --script

(import (chezscheme) (sbral-tests) (substitution-tests) (state-tests) (runner-tests))

(run-sbral-tests)
(run-substitution-tests)
(run-state-tests)
(run-runner-tests)

(display "Testing Complete\n")
